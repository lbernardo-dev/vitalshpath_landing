---
import { ArrowRight, Apple, PlayCircle } from "lucide-react";
import { useTranslations } from "../../i18n/ui";

const { lang = 'es' } = Astro.props;
const t = useTranslations(lang);
---

<section class="relative min-h-screen flex flex-col justify-center pt-32 pb-20 overflow-hidden">
  
  <!-- Floating Orbs (Background Animation 2.0) -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
      <!-- Purple Orb -->
      <div class="absolute top-[10%] left-[20%] w-[500px] h-[500px] bg-purple-400/20 rounded-full blur-[100px] animate-float opacity-60"></div>
      <!-- Blue Orb -->
      <div class="absolute bottom-[20%] right-[10%] w-[600px] h-[600px] bg-blue-400/20 rounded-full blur-[120px] animate-breathe opacity-50"></div>
  </div>

  <div class="container mx-auto px-6 relative z-10 text-center">
    
    <!-- "Pill" Badge -->
    <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/50 backdrop-blur-xl border border-white/60 shadow-sm mb-8 animate-reveal" style="animation-delay: 0s">
        <span class="w-2 h-2 rounded-full bg-[#007aff]"></span>
        <span class="text-xs font-semibold tracking-wide uppercase text-[#1d1d1f]/70">iOS 18 Ready</span>
    </div>

    <!-- Massive Heading -->
    <h1 class="text-hero mb-8 animate-reveal" style="animation-delay: 0.1s">
      Salud familiar. <br />
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-[#007aff] to-[#af52de]">Redefinida.</span>
    </h1>

    <!-- Subtitle -->
    <p class="text-body-lg max-w-2xl mx-auto mb-10 animate-reveal" style="animation-delay: 0.2s">
      Una experiencia diseñada con precisión quirúrgica y estética espacial. 
      Gestiona expedientes, tomas y citas con una fluidez que nunca has sentido.
    </p>

    <!-- Actions -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-6 mb-24 animate-reveal" style="animation-delay: 0.3s">
        <a href="#" class="btn-spatial group">
            <Apple className="w-5 h-5 mr-2 -mt-1" />
            <span>App Store</span>
        </a>
        <a href="#demo" class="flex items-center gap-2 text-[#007aff] font-medium hover:underline text-lg transition-all">
            <PlayCircle className="w-5 h-5" />
            <span>Ver demo interactiva</span>
        </a>
    </div>

    <!-- The "Spatial" Phone Presentation -->
    <div class="relative w-full max-w-6xl mx-auto h-[600px] md:h-[800px] animate-reveal" style="animation-delay: 0.5s">
        
        <!-- Main Device (Center) -->
        <div class="absolute left-1/2 top-0 -translate-x-1/2 w-[320px] md:w-[400px] aspect-[9/19.5] glass-panel p-2 z-20">
             <div class="w-full h-full rounded-[28px] overflow-hidden bg-black shadow-inner">
                <img src={`${import.meta.env.BASE_URL}/assets/images/screens/hero-clinical-v1.jpg`} class="w-full h-full object-cover" alt="App Main" />
             </div>
        </div>

        <!-- Floating Card Left (Glass) -->
        <div class="absolute left-[5%] md:left-[15%] top-[20%] w-64 p-6 glass-panel z-10 animate-float hidden md:block">
            <div class="flex items-center gap-4 mb-3">
                <div class="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center text-blue-600">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path d="M22 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg>
                </div>
                <div>
                    <div class="text-xs font-bold text-gray-400 uppercase">Perfil</div>
                    <div class="font-semibold text-gray-900">Abuela Maria</div>
                </div>
            </div>
            <div class="h-1.5 w-full bg-gray-100 rounded-full overflow-hidden">
                <div class="h-full bg-blue-500 w-[85%]"></div>
            </div>
            <div class="mt-2 text-xs text-gray-500 flex justify-between">
                <span>Adherencia</span>
                <span class="font-bold text-gray-900">85%</span>
            </div>
        </div>

        <!-- Floating Card Right (Glass) -->
        <div class="absolute right-[5%] md:right-[15%] bottom-[30%] w-64 p-6 glass-panel z-30 animate-float" style="animation-delay: 2s">
             <div class="flex items-center justify-between mb-4">
                 <span class="text-xs font-bold text-gray-400 uppercase">Próxima Dosis</span>
                 <span class="text-xs font-bold text-red-500 bg-red-50 px-2 py-1 rounded-full">10:00 AM</span>
             </div>
             <div class="flex items-center gap-3">
                 <div class="w-12 h-12 rounded-xl bg-orange-100 flex items-center justify-center text-orange-600">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m10.5 20.5 10-10a4.95 4.95 0 1 0-7-7l-10 10a4.95 4.95 0 1 0 7 7Z"></path></svg>
                 </div>
                 <div>
                     <div class="font-bold text-gray-900">Ibuprofeno</div>
                     <div class="text-xs text-gray-500">400mg • Con comida</div>
                 </div>
             </div>
        </div>

    </div>

  </div>
</section>
